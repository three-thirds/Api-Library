# Ping

This is the health check. It does not call any outside service. If the worker is up, you get a small JSON body back.

/api/v1/ping

- ok is true when the route is alive
- message is the string pong
